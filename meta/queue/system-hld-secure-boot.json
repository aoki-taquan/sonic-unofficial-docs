{
  "page": "docs/system/hld-secure-boot.md",
  "issue": 712,
  "verification": "discrepancy-found",
  "concerns": [
    "Phase 2 (userland binary / container image 署名 IMA 等) の議論状況確認"
  ],
  "verified_concerns": [
    "署名スクリプトは .py ではなく bash 版 (sonic-buildimage/scripts/signing_secure_boot_dev.sh) として取り込まれている",
    "build flag は SB_BUILD ではなく SECURE_UPGRADE_MODE と SECURE_UPGRADE_DEV_SIGNING_KEY (rules/config:287-294, build_image.sh:264, Makefile.work:366-376)",
    "Production 用は固定スクリプトではなく SECURE_UPGRADE_PROD_SIGNING_TOOL 変数で外部ツールを差し替える方式 (Makefile.work:376-383)",
    "shim → grub → vmlinuz → KO chain は scripts/signing_secure_boot_dev.sh と secure_boot_signature_verification.sh で実装"
  ],
  "priority": "low",
  "notes": "discrepancy-found: HLD は signing_secure_boot_dev.py / SB_BUILD と書いているが実装は .sh / SECURE_UPGRADE_MODE。boot chain の verify 思想は HLD どおり。verified 2026-05-10 (verifier batch-23)."
}
