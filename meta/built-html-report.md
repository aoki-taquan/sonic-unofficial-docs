# Built HTML quality report

`mkdocs build --strict` 後の `site/` を `meta/scripts/check_built_html.py` で走査した結果。

## Summary

- pages scanned: **903**

### High priority

- `<h1>` duplicated pages: **0**
- empty `<a href="">`: **0**
- empty `<img src="">`: **0**

### Low priority (report only)

- `<title>` missing: **0**
- `<h1>` missing: **0**
- empty `<pre><code></code></pre>`: **0**
- mermaid placeholder residue: **0**

## Pages with any signal (0)

- (none)

